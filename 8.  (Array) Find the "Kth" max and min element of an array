class Solution {
public:
    int kthSmallest(int arr[], int l, int r, int k) {
        // std::set<int> res;
        sort(arr, arr+(r+1));
        
        int result = arr[k-1];
          return result;
    }
  
};
